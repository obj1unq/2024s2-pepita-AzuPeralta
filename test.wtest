import example.*

describe "pepita" {
  test "pepita come alpiste y aumenta su energia" {
    pepita.comer(alpiste)
    assert.equals(120, pepita.energia()) 
    
    
    pepita.comer(manzana)
    assert.equals(125, pepita.energia())
  }
  
  test "Pepita vuela y disminuye su energia" {
    assert.equals(100, pepita.energia())
    
    pepita.volar(10)
    
    assert.equals(80, pepita.energia())
  }
}
//assert objeto de wollok que espera dos argumentos, lo que espera y el metodo que corrobora//assert objeto de wollok que espera dos argumentos, lo que espera y el metodo que corrobora


